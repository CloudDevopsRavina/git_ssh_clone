we need to generate a key which is ssh-keygen and then this public key we need to update here in github

Github----profile----setting-----ssh & gpg key----new sshkey ----give any totle---paste inside key


git clone <ssh link>
git add .---which mean all file in currentry will be move to stagging area
git commit -m "all"  ----local repository
git push  ---remote repository


Working dirrectory ----------stagging area-----local repositoy ------remote repository

